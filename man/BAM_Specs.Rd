% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bam-specs.R
\docType{data}
\name{BAM_Specs}
\alias{BAM_Specs}
\title{Specifications for Importing BAM Assessments}
\format{
\code{BAM_Specs} is a named list, where the names correspond to a stock assessment
available in the \code{bamExtras} package (i.e., no spaces in name, see \code{\link[MSEtool:ImportBAM]{MSEtool::ListBAMStocks()}}).

Each element in \code{BAM_Specs} is a named list with the following named elements:
\itemize{
\item \code{Stock} - \code{character(1)}The name of the stock (typically same as the name
of the parentlist element, but includes spaces)
\item \code{SEDAR} - \code{numeric(1)}The SEDAR assessment number. Optional.
\item \code{Link} - URL to the SEDAR stock assessment report. Optional but recommended.
\item \code{SurveyNames} - A character vector of user-facing names for survey
indices detected in \code{BAMdata}. Must have the same length as the number of
survey indices found (i.e. indices not matched to any fleet name). Use
this to override the default names derived from column names.
\item \code{UnitsLandings} - A character vector of length equal to the number of
landings fleets detected. Required.
Each element must be one of:
\itemize{
\item \code{"1000 lb"} — values will be converted from thousands of pounds to kg.
\item \code{"1000 n"} — values will be multiplied by 1,000 (number in thousands).
}
\item \code{UnitsDiscards} - A character vector of length equal to the number of
discard fleets detected. Accepts the same values as \code{UnitsLandings}.
\item \code{DiscMortDF} - \code{data.frame} with columns Fleet, Value, and Year
specifying discard mortality rates by fleet and time block. Required in some
cases.
\item \code{DiscFleets} - Named character vector mapping retain fleet names
to their corresponding discard fleet names in the BAM output, for cases
where naming conventions differ. Required in some cases.
\item \code{DiscSelFleets} - Named character vector mapping retain fleet
names to the selectivity series to use for discards. Required in some
cases. Required in some cases.
\item \code{RetSelFleets} - Named character vector mapping retain fleet
names to alternative retention selectivity series. Required in some cases.
}
}
\usage{
BAM_Specs
}
\description{
\code{BAM_Specs} is a named list of specifications for importing BAM output into
an operating model object (see \code{\link[MSEtool:OM]{MSEtool::OM()}}).
}
\examples{
names(BAM_Specs)

BAM_Specs[[1]]


}
\keyword{datasets}
