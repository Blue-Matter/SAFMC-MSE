% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-oms.R
\name{Import_OMs}
\alias{Import_OMs}
\title{Import BAM Operating Models}
\usage{
Import_OMs(
  Import_Stocks = NULL,
  Compare = TRUE,
  plot = FALSE,
  save = TRUE,
  OMpath = "Objects/OM/SingleStock"
)
}
\arguments{
\item{Import_Stocks}{\code{character}. Names of stocks to import. If \code{NULL}
(default), all stocks defined in \code{BAM_Specs}}

\item{Compare}{\code{logical}. If \code{TRUE} (default), runs
\code{\link[MSEtool:CompareBAM]{MSEtool::CompareBAM()}} on each imported OM to validate against BAM
reference points.}

\item{plot}{\code{logical}. If \code{TRUE}, diagnostic plots are produced during
\code{\link[MSEtool:CompareBAM]{MSEtool::CompareBAM()}}. Passed directly to that function. Defaults to
\code{FALSE}.}

\item{save}{\code{logical}. Save the OM to disk? Default TRUE}

\item{OMpath}{\code{character}. Directory where \code{.om} files are saved.
Defaults to \code{"Objects/OM/SingleStock"}.}
}
\value{
Called for its side effects. Each imported OM is saved to \code{OMpath}
as \verb{<StockName>.om}. Returns list of imported OMs invisibly.
}
\description{
Imports one or more stock Operating Models (OMs) from BAM (Beaufort
Assessment Model) output via \code{\link[MSEtool:ImportBAM]{MSEtool::ImportBAM()}}, optionally validates
them against BAM population dynamics with \code{\link[MSEtool:CompareBAM]{MSEtool::CompareBAM()}}, and saves
each OM to disk as a \code{.om} file.
}
\details{
Project-wide settings \code{nSim}, \code{pYear}) are loaded at the
start of the function via \code{\link[=Settings]{Settings()}}. Each stock's BAM configuration
is drawn from \link{BAM_Specs}.
}
\seealso{
\code{\link[=Settings]{Settings()}}, \code{\link[MSEtool:ImportBAM]{MSEtool::ImportBAM()}}, \code{\link[MSEtool:CompareBAM]{MSEtool::CompareBAM()}}
}
