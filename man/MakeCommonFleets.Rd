% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make-common-fleets.R
\name{MakeCommonFleets}
\alias{MakeCommonFleets}
\title{Make Common Fleets Across Operating Models}
\usage{
MakeCommonFleets(
  OM_List = NULL,
  CommonFleets,
  StockFleetList,
  OMpath = "Objects/OM",
  save = TRUE
)
}
\arguments{
\item{OM_List}{Optional named list of single-stock \code{om} objects. If \code{NULL}
(default), OMs are loaded from \code{OMpath} via \code{\link[=LoadOM]{LoadOM()}}.}

\item{CommonFleets}{Character vector of fleet names that every OM must
contain after processing, in the desired order.}

\item{StockFleetList}{Named list where each element corresponds to a stock
in \code{OM_List} (matched by name) and contains a fleet list passed to
\code{\link[MSEtool:CombineFleets]{MSEtool::CombineFleets()}}. Used to combine multiple fleets within each
stock into a single fleet.}

\item{OMpath}{Path to the directory containing OM objects. Used both for
loading (when \code{OM_List = NULL}) and saving. Defaults to
\code{"Objects/OM"}.}

\item{save}{Logical. If \code{TRUE} (default), each processed OM is saved to
\verb{<OMpath>/MultiStock/<StockName>.om}.}
}
\value{
A named list of processed \code{om} objects, returned invisibly. Each OM
has fleets combined per \code{StockFleetList}, dummy fleets added for any
missing \code{CommonFleets}, and all fleets reordered to match \code{CommonFleets}.

Processing proceeds in three steps:
\enumerate{
\item \strong{Combine fleets} — for each stock named in \code{StockFleetList},
\code{\link[MSEtool:CombineFleets]{MSEtool::CombineFleets()}} is called with the supplied fleet list.
\item \strong{Add dummy fleets} — \code{\link[=MissingFleets]{MissingFleets()}} identifies any \code{CommonFleets}
absent from each OM; \code{\link[MSEtool:AddFleet]{MSEtool::AddFleet()}} adds an empty placeholder for
each.
\item \strong{Reorder} — \code{\link[MSEtool:Reorder]{MSEtool::Reorder()}} reorders every OM's fleets to match
\code{CommonFleets} exactly.
}

An error is thrown if, after steps 1 and 2, any OM contains fleets not in
\code{CommonFleets}, or if any name in \code{StockFleetList} is not found in
\code{OM_List}.
}
\description{
Combines fleets within each single-stock OM, adds dummy fleets where
required, and reorders all OMs so they share an identical fleet structure.
Optionally saves the resulting OM objects to disk.
}
\seealso{
\code{\link[=LoadOM]{LoadOM()}}, \code{\link[=MissingFleets]{MissingFleets()}}, \code{\link[MSEtool:Reorder]{MSEtool::Reorder()}},
\code{\link[MSEtool:CombineFleets]{MSEtool::CombineFleets()}}, \code{\link[MSEtool:AddFleet]{MSEtool::AddFleet()}}
}
